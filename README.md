
  # Folder structure setup

  This is a code bundle for Folder structure setup. The original project is available at https://www.figma.com/design/cydDaeaH1sPruRZBGYeTlr/Folder-structure-setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  